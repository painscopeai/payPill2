import ProviderToolPlaceholder from '@/views/ProviderToolPlaceholder.jsx';

export default function ProviderIntegrationsPage() {
	return (
		<ProviderToolPlaceholder
			title="Integrations"
			description="Connect EHR, billing, and lab systems. Configuration UI will list available connectors as they are enabled for your tenant."
		/>
	);
}
