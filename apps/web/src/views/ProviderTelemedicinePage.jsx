import ProviderToolPlaceholder from '@/views/ProviderToolPlaceholder.jsx';

export default function ProviderTelemedicinePage() {
	return (
		<ProviderToolPlaceholder
			title="Telemedicine"
			description="Join visits tied to patient appointments. Session scheduling uses the same appointments table as patients and admins."
		/>
	);
}
