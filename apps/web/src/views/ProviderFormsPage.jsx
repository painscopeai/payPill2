import ProviderToolPlaceholder from '@/views/ProviderToolPlaceholder.jsx';

export default function ProviderFormsPage() {
	return (
		<ProviderToolPlaceholder
			title="Forms"
			description="Assign intake and consent forms linked to the shared PayPill forms engine."
		/>
	);
}
